# Backend base URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:3000

# Public site URL for meeting links in booking confirmation (no trailing slash)
VITE_PUBLIC_APP_URL=http://localhost:5173

# Default host username for /book when path has no segments
VITE_DEFAULT_HOST_USERNAME=priyanshu
